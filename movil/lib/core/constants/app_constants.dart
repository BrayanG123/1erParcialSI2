

class AppConstants {
  // Radio en km para buscar mecánicos cercanos
  static const double radioBusquedaKm = 10.0;

  // Intervalo de polling para actualizar posición (en segundos)
  static const int intervaloPollingSegundos = 5;

  // Zoom por defecto del mapa
  static const double zoomMapa = 15.0;

  // Tamaño máximo de foto en MB antes de comprimir
  static const double maxFotoMb = 5.0;

  // Duración máxima de audio en segundos
  static const int maxAudioSegundos = 120;
}